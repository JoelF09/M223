diff --git a/node_modules/sichere-express-actions/packages/application/src/resourceService.js b/node_modules/sichere-express-actions/packages/application/src/resourceService.js
index 3cdcd3e..4e65bd2 100644
--- a/node_modules/sichere-express-actions/packages/application/src/resourceService.js
+++ b/node_modules/sichere-express-actions/packages/application/src/resourceService.js
@@ -331,7 +331,12 @@ export class ResourceService {
         : { [config.actorField]: current[config.actorField] };
       return serializeResource(this.resource, await this.repository.updateWhere(id, changes, expected, { concurrency: false }));
     };
-    return this.database?.transaction ? this.database.transaction(operation) : operation();
+    const result = this.database?.transaction ? await this.database.transaction(operation) : await operation();
+    // PATCH (siehe patches/): Sperren/Entsperren loeste bisher kein Live-Update
+    // aus - andere bereits offene Fenster sahen den Sperrstatus erst nach
+    // manuellem Neuladen, nicht sofort ueber SSE.
+    await this.publish(`${kind}:${acquire ? 'acquire' : 'release'}`, result);
+    return result;
   }
 
   acquireLock(id, context) { return this.changeLease('locked', id, true, context); }
diff --git a/node_modules/sichere-express-actions/packages/react/src/components.js b/node_modules/sichere-express-actions/packages/react/src/components.js
index fe4ea90..e748972 100644
--- a/node_modules/sichere-express-actions/packages/react/src/components.js
+++ b/node_modules/sichere-express-actions/packages/react/src/components.js
@@ -161,19 +161,28 @@ export function ResourceTable({ resource, records = [], onEdit, onDelete, onTran
         metadata.editingLock && record[metadata.editingLock.actorField] != null
           && Date.parse(record[metadata.editingLock.atField]) + metadata.editingLock.ttlMs > Date.now()
           && h('span', { className: 'sichere-lock', title: record[metadata.editingLock.atField] },
-            `Gesperrt von ${leaseActorName(metadata, metadata.editingLock, record, referenceOptions)} seit ${record[metadata.editingLock.atField]} `),
+            // PATCH (siehe patches/): Zeitpunkt roh als ISO-String angezeigt
+            // ("seit 2026-08-31T08:35:31.667Z") statt normal formatiert.
+            `Gesperrt von ${leaseActorName(metadata, metadata.editingLock, record, referenceOptions)} seit ${historyDate(record[metadata.editingLock.atField])} `),
         metadata.reservation && record[metadata.reservation.actorField] != null
           && Date.parse(record[metadata.reservation.atField]) + metadata.reservation.ttlMs > Date.now()
           && h('span', { className: 'sichere-reservation', title: record[metadata.reservation.atField] },
-            `Vorgemerkt von ${leaseActorName(metadata, metadata.reservation, record, referenceOptions)} seit ${record[metadata.reservation.atField]} `),
-        onEdit && (!canRecord || canRecord('update', record)) && h(EditButton, {
-          onClick: () => onEdit(record),
-          disabled: metadata.editingLock && record[metadata.editingLock.actorField] != null
+            `Vorgemerkt von ${leaseActorName(metadata, metadata.reservation, record, referenceOptions)} seit ${historyDate(record[metadata.reservation.atField])} `),
+        onEdit && (!canRecord || canRecord('update', record)) && (() => {
+          // PATCH (siehe patches/): title stand bisher fest auf jedem
+          // Bearbeiten-Button, auch wenn disabled korrekt false war (sogar
+          // bei Resources ganz ohne editingLock) - jetzt nur noch, wenn
+          // tatsaechlich eine fremde aktive Sperre besteht.
+          const gesperrtDurchAndere = Boolean(metadata.editingLock && record[metadata.editingLock.actorField] != null
             && Date.parse(record[metadata.editingLock.atField]) + metadata.editingLock.ttlMs > Date.now()
             && String(record[metadata.editingLock.actorField]) !== String(user?.id)
-            && !metadata.editingLock.overrideRoles.some((role) => userRoles(user).has(role)),
-          title: 'Eine fremde aktive Sperre verhindert die Bearbeitung',
-        }),
+            && !metadata.editingLock.overrideRoles.some((role) => userRoles(user).has(role)));
+          return h(EditButton, {
+            onClick: () => onEdit(record),
+            disabled: gesperrtDurchAndere,
+            title: gesperrtDurchAndere ? 'Eine fremde aktive Sperre verhindert die Bearbeitung' : undefined,
+          });
+        })(),
         onDelete && (!canRecord || canRecord('delete', record)) && h(DeleteButton, { onClick: () => onDelete(record) }),
         onHistory && h('button', { type: 'button', 'data-operation': 'history', onClick: () => onHistory(record) }, 'Historie'),
         ...(onTransition ? Object.entries(workflowTransitions)
@@ -248,7 +257,9 @@ export function ConflictDialog({ conflict, onReload, onForce, onCancel }) {
   return h('div', { role: 'alertdialog', 'aria-modal': true, 'data-view': 'conflict' },
     h('p', null, 'Dieser Datensatz wurde zwischenzeitlich geaendert.'),
     conflict.changedBy != null && h('p', null, `Geaendert von: ${conflict.changedByName ?? conflict.changedByActor?.name ?? conflict.changedBy}`),
-    conflict.changedAt && h('p', null, `Geaendert am: ${conflict.changedAt}`),
+    // PATCH (siehe patches/): Zeitpunkt roh als ISO-String angezeigt statt
+    // normal formatiert (dieselbe historyDate()-Funktion wie sonst im Framework).
+    conflict.changedAt && h('p', null, `Geaendert am: ${historyDate(conflict.changedAt)}`),
     names.length > 0 && h('table', null,
       h('thead', null, h('tr', null, h('th', null, 'Feld'), h('th', null, 'Aktuell'), h('th', null, 'Meine Eingabe'))),
       h('tbody', null, ...names.map((name) => h('tr', { key: name },
diff --git a/node_modules/sichere-express-actions/packages/sqlite/src/repository.js b/node_modules/sichere-express-actions/packages/sqlite/src/repository.js
index d88cc31..e5b6384 100644
--- a/node_modules/sichere-express-actions/packages/sqlite/src/repository.js
+++ b/node_modules/sichere-express-actions/packages/sqlite/src/repository.js
@@ -125,6 +125,11 @@ export function createResourceRepository({ db, resource }) {
           const current = this.findById(id);
           if (!current) throw new NotFoundError();
           throw new ConflictError('Ressource wurde zwischenzeitlich geaendert', {
+            // PATCH (siehe patches/): stabiler Code fuer die Frontend-Logik -
+            // unabhaengig vom (uebersetzbaren) message-Text soll die UI einen
+            // echten Versionskonflikt von anderen 409-Ursachen (z.B. "Auto
+            // bereits vermietet") unterscheiden koennen.
+            code: optimistic ? 'VERSIONSKONFLIKT' : undefined,
             current, submitted: input, currentData: current, submittedData: input,
             currentVersion: optimistic ? current[versionField] : undefined,
             changedAt: resource.tracking ? current[resource.tracking.updatedAt] : undefined,
